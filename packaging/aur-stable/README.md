# Arch Linux (AUR stable)

This folder contains the stable AUR package metadata for `wayland-scroll-factor`.

Build locally:

```
cd packaging/aur-stable
makepkg -si
```

This package builds from the latest stable GitHub tag tarball. For the live Git
package, use `packaging/aur` / `wayland-scroll-factor-git`.
